{
  "name": "jsondiscovery-ado-pipelines",
  "version": "0.1.3",
  "description": "Browser (Chrome, Edge, Firefox) extension for ADO pipeline discovery",
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js src scripts",
    "dev": "node ./scripts/build.js",
    "build": "NODE_ENV=production node ./scripts/build.js && node ./scripts/zip.js"
  },
  "devDependencies": {
    "@discoveryjs/discovery": "1.0.0-beta.70",
    "@discoveryjs/json-ext": "^0.5.7",
    "css-tree": "^2.1.0",
    "esbuild": "^0.14.49",
    "eslint": "^8.10.0",
    "jszip": "^3.7.1",
    "mime": "^3.0.0"
  }
}
